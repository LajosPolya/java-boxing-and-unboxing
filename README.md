# java-boxing-and-unboxing

Interface to test boxing and unboxing primitives in Java.

## Build and Run

```shell
# build
mvn clean install
```

```shell
# run with JFR
java -XX:StartFlightRecording:filename=test.jfr -jar target/java-boxing-and-unboxing-1.0-SNAPSHOT.jar
```
